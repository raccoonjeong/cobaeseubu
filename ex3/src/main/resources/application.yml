spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/bootex
    username: bootuser
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  thymeleaf:
    cache: false